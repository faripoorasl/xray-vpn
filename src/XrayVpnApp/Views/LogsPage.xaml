<UserControl x:Class="XrayVpnApp.Views.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="↻ Refresh" Click="Refresh_Click" Padding="12,8" Margin="0,0,8,0"/>
            <Button Content="🧹 Clear" Click="Clear_Click" Padding="12,8" Margin="0,0,8,0"/>
            <Button Content="📂 Open Folder" Click="OpenFolder_Click" Padding="12,8"/>
        </WrapPanel>

        <Border Grid.Row="1" Background="#1A1A1A" CornerRadius="4" ClipToBounds="True">
            <TextBox x:Name="LogBox"
                     FontFamily="Cascadia Code, Consolas, Courier New"
                     FontSize="12"
                     Background="Transparent"
                     Foreground="#A0FFA0"
                     BorderThickness="0"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap"
                     Padding="12"/>
        </Border>

        <TextBlock Grid.Row="2" x:Name="LogPathLabel"
                   Foreground="{StaticResource TextMutedBrush}" FontSize="11"
                   Margin="0,8,0,0"/>
    </Grid>
</UserControl>
